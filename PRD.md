# 「今天吃什么」产品需求文档（PRD）

## 1. 文档信息

- 产品暂定名：今天吃什么
- 文档版本：V0.2
- 文档状态：已评审，进入产品设计阶段
- 首发形态：手机优先的网页应用，兼容桌面浏览器
- 首发范围：中国大陆，外出推荐先覆盖深圳市
- 发布性质：小范围公开测试版
- 商业化状态：免费、无广告、无付费排序、无应用内支付

本文档描述产品需要提供的能力、规则与验收标准，不指定具体前端框架、数据库、推荐模型实现、AI 服务商、地图服务商或其他第三方 API。

---

## 2. 产品定位

「今天吃什么」是一款以“可自定义的盲盒菜单”为核心体验的饮食决策产品。

产品的首要价值不是帮助用户搜索更多菜谱，而是帮助用户在少量必要条件下快速做出合理的用餐决定，并优先利用已有或临期食材，减少食材闲置和浪费。

推荐结果必须先满足安全和必要约束，再保留适度随机性。产品不进行完全无规则的随机，也不要求用户每次完成大量筛选。

第一版包含三项相互关联但有明确主次的能力：

1. 居家库存推荐：根据个人库存生成可制作的一道菜或一顿菜单，是产品主线。
2. 随机菜单：在不要求使用库存的情况下，根据人数和偏好生成菜单，是主线的补充能力。
3. 外出推荐：帮助用户在深圳决定吃什么菜系或选择真实餐厅，是独立次线。

---

## 3. 目标用户

### 3.1 核心目标用户

居住或工作在深圳、主要为一至两人用餐的年轻上班族。他们会在家制作日常饭菜，但库存维护不规律，经常因为不知道吃什么而让食材闲置；同时也存在工作日午餐、晚餐外出就餐的需求。

### 3.2 非首要目标用户

以下人群可以使用产品，但第一版不以满足其全部专业需求为目标：

- 多成员协同管理库存的家庭；
- 需要严格营养计算的专业健身人群；
- 需要医疗级膳食管理的用户；
- 专业厨师或餐饮从业者；
- 需要宴请、节庆菜单或专业烘焙内容的用户；
- 深圳以外需要真实餐厅推荐的用户。

---

## 4. 核心问题

产品需要解决以下问题：

1. 用户已经拥有一些食材，但不知道能做什么。
2. 用户难以判断哪些食材应优先消耗，导致临期或过期浪费。
3. 用户不愿每次设置大量筛选条件，只想快速得到一个合理结果。
4. 多人用餐时，用户不知道菜品数量、组合和食材分量如何安排。
5. 用户库存不足时，不知道是换一道菜还是少量补买。
6. 用户外出时，可能只知道地点和大致偏好，却仍难以决定菜系或餐厅。
7. 传统完全随机缺乏实用性，传统搜索和筛选又会增加决策成本。

---

## 5. 产品目标与测试目标

### 5.1 第一版产品目标

- 让游客无需注册即可快速完成一次有效推荐。
- 帮助用户建立并持续维护轻量个人库存。
- 让库存、临期状态、偏好和人数真实影响推荐结果。
- 让用户能够从推荐到制作，再到库存扣减，完成闭环。
- 在外出场景下，基于真实位置或指定区域提供真实餐厅选择。
- 在满足约束的前提下保留盲盒式趣味和轻微仪式感。

### 5.2 小范围公开测试的核心验证目标

验证用户是否愿意持续维护轻量库存，并实际采用基于库存生成的推荐，从而更优先地消耗已有或临期食材。

### 5.3 测试期主要观察指标

- 首次使用完成食材录入并获得有效推荐的情况；
- 推荐后点击“我做了这顿饭”的情况；
- 用户确认库存扣减的情况；
- 临期食材进入推荐并被确认消耗的情况；
- 用户后续再次打开应用并更新库存的情况；
- 重抽、“今天不想吃”、“不喜欢”和“不再推荐”的使用情况；
- 外出推荐后查看地图或路线的情况；
- 无结果、AI 失败、定位失败和第三方餐厅数据失败的发生情况。

测试期先建立真实基线，不以缺乏历史依据的注册量或浏览量目标作为唯一成功标准。

---

## 6. 产品原则

### 6.1 先合理，后随机

推荐先通过硬约束和最低可用性检查，再在合格候选中进行加权随机。

### 6.2 减少首次使用负担

游客可直接使用。首次体验只要求填写会立即影响本次结果的信息，完整库存和偏好可在后续逐步补充。

### 6.3 安全约束优先

过敏原、明确不吃和饮食禁忌不得为了产生结果而被自动放宽。

### 6.4 不制造虚假精度

模糊库存继续使用约数表达；营养数据标记为估算；未知营业状态不得显示为正在营业；缺失数据不得被系统擅自补成确定事实。

### 6.5 推荐可解释

结果应说明主要推荐原因，例如库存匹配程度、临期食材利用、符合人数或满足某项偏好。

### 6.6 用户确认后再改变数据

抽到菜谱、查看详情或调用 AI 都不会自动扣减库存。只有用户确认实际制作并确认预计消耗后，库存才会更新。

### 6.7 核心功能不依赖 AI 可用

结构化推荐、库存管理和菜谱查看在 AI 服务不可用时仍需正常使用。

---

## 7. 用户角色

### 7.1 游客

- 无需登录即可管理本地库存、设置本地偏好、获取推荐、收藏、屏蔽和创建私有菜谱；
- 数据保存在当前设备；
- 更换设备、清除浏览器数据或使用无痕模式可能导致数据丢失；
- 登录时可以将本地数据合并至云端账号。

### 7.2 登录用户

- 使用中国大陆手机号和短信验证码登录，不设置密码；
- 库存、偏好、收藏、屏蔽、自定义菜谱和必要历史记录保存到云端；
- 支持跨设备使用；
- 可以注销账户并申请删除个人数据。

### 7.3 内部内容管理员

- 仅授权内部人员使用；
- 可以维护系统菜谱、标准食材、别名、分类、单位和过敏原标记；
- 可以修正营养估算和菜谱错误；
- 可以将明显错误、停业或不适合推荐的餐厅加入屏蔽名单；
- 可以查看用户主动提交的问题反馈；
- 内容修改需要保留修改者和修改时间。

第一版不包含家庭成员、共享库存或公开内容创作者角色。

---

## 8. 使用场景

### 8.1 居家：使用现有库存

用户下班回家，希望使用冰箱里的鸡蛋、西红柿和青菜解决晚餐。系统优先推荐能消耗临期青菜的组合，并说明已有食材匹配程度。

### 8.2 居家：允许少量补买

用户愿意额外购买少量食材。系统可以推荐库存高度匹配的菜单，并明确列出缺少的关键食材、普通配料和可选配料。

### 8.3 居家：不参考库存

用户准备重新买菜，直接按人数、菜系、饮食偏好和营养目标抽取菜单。

### 8.4 外出：工作地点附近就餐

用户在单位附近选择午餐，通过当前定位或收藏地点抽取菜系或真实餐厅。

### 8.5 外出：指定区域或旅行途中就餐

用户手动选择深圳某个区域、商圈或地点，在该范围内抽取符合条件的真实餐厅。

第一版只覆盖午餐和晚餐，可选择简餐或正常正餐，不专门覆盖早餐、下午茶、夜宵、宴请和节庆菜单。

---

## 9. 核心用户流程

### 9.1 首次使用流程

1. 用户进入产品。
2. 用户选择“在家吃”或“外出吃”。
3. 居家用户可快速添加少量常见食材，也可跳过并使用随机菜单。
4. 产品优先询问过敏原、明确不吃和必要饮食禁忌。
5. 其他偏好、营养目标和账号登录均可稍后完成。
6. 用户发起推荐并查看结果。

### 9.2 库存推荐流程

1. 用户选择用餐人数和午餐/晚餐。
2. 用户选择简餐、正常正餐或“只抽一道菜”。
3. 用户选择“尽量只用现有食材”或“允许补买少量”。
4. 用户可选设置菜系、辣度、烹饪时间、营养目标，以及在“允许补买少量”模式下设置居家补买预算。
5. 系统过滤不符合硬约束的菜谱和菜单组合。
6. 系统根据库存匹配、临期程度、营养搭配和个人偏好形成候选集。
7. 系统从合格候选中加权随机并展示结果和推荐理由。
8. 用户接受结果、查看菜谱、调用受限制的 AI 辅助，或选择再来一次。
9. 实际制作后，用户点击“我做了这顿饭”。
10. 系统展示预计扣减项，用户调整后确认更新库存。

### 9.3 随机菜单流程

1. 用户选择不参考库存。
2. 用户设置人数、餐次、简餐/正餐及少量必要条件。
3. 系统根据硬约束、营养搭配和软偏好形成候选菜单。
4. 系统加权随机生成一顿饭，并展示菜谱和按人数估算的用量。
5. 用户可以收藏、反馈、重抽或切换回库存模式。

### 9.4 外出推荐流程

1. 用户选择“附近就餐”或“指定区域”。
2. 附近就餐由用户主动授权定位；指定区域无需定位。
3. 定位失败或被拒绝时，用户可以手动选择区域、商圈或地点。
4. 用户选择直接抽菜系、先选菜系再抽餐厅，或直接抽餐厅。
5. 系统按硬约束、距离、外出人均预算、营业信息可靠度和历史偏好过滤及排序。
6. 系统从合格候选中加权随机推荐真实餐厅。
7. 用户查看餐厅基础信息，并可跳转外部地图查看路线。
8. 用户可以再来一次或提交信息错误反馈。

### 9.5 游客登录与数据合并流程

1. 游客选择使用手机号登录。
2. 用户通过短信验证码完成身份验证。
3. 若账号无云端数据，本地数据直接迁移至账号。
4. 若账号已有数据，系统展示合并说明，避免静默覆盖。
5. 合并完成后，本地与云端状态一致；失败时保留原数据并提供重试。

---

## 10. 功能需求

### 10.1 首屏与场景选择

- 首屏提供清晰的“在家吃”和“外出吃”入口；
- 居家主线在信息层级上优先，但不妨碍用户直接选择外出；
- 页面只保留一个明确的主操作，避免同时展示大量高级筛选；
- 用户返回时保留最近的场景、人数和非敏感选择，但不得绕过硬约束确认。

### 10.2 食材库存

每个库存批次支持以下信息：

- 标准化食材名称；
- 用户输入的原始名称或备注；
- 食材分类；
- 大致数量或重量；
- 单位；
- 可选购买时间或录入时间；
- 可选预计过期时间；
- 可选“尽快食用”标记；
- 库存状态，如正常、临期、已用完；
- 数据所有者与创建、更新时间。

规则：

- 食材名称为唯一必填项；
- 数量和日期可选，缺失时不得假装精确；
- 同一种食材可以存在多个批次；
- 再次添加时允许合并，也允许保留新批次；
- 临期推荐优先基于用户填写的预计过期时间或“尽快食用”标记；
- 不因食材类别自动断言某批次已变质；
- 用户可以手动编辑、用完、恢复或移除库存；
- 第一版不支持家庭共享库存。

### 10.3 常备基础项

- 产品提供一组可编辑的常备基础调料；
- 用户可以启用、停用和补充常备项；
- 常备项默认不要求维护精确余量；
- 菜谱仍需展示大致用量；
- 咖喱块、火锅底料、淡奶油等决定菜品成立的特殊配料不作为默认常备项；
- 过敏原和明确禁忌始终优先于常备项设置。

### 10.4 临期提醒

- 第一版只提供应用内提醒；
- 首页展示临期和“尽快食用”食材；
- 用户可以设置提前多少天视为临期；
- 无日期且未标记的食材不自动进入临期提醒；
- 第一版不提供短信、浏览器后台推送或微信提醒。

### 10.5 用餐人数与结果形式

- 用户必须能够选择用餐人数；
- 一人用餐默认优先简餐、盖饭、面食或少量菜品组合；
- 多人用餐默认提供主菜、配菜与主食的组合菜单；
- 用户可切换为“只抽一道菜”；
- 菜品用量随人数调整；
- 调味料、汤汁和烹饪损耗不得机械地完全等比例放大；
- 菜单组合需避免明显重复、全荤、全素或主食缺失，除非用户偏好要求如此。

### 10.6 库存匹配模式

产品提供两种模式：

#### 尽量只用现有食材

- 只推荐不存在明确“数量不足”的结果；库存数量为“可能满足”或“未知”时可以进入候选，但必须提示用户先确认实际余量；
- 无结果时说明原因，并建议补充库存信息或主动切换模式；
- 系统不得暗中放宽库存条件。

#### 允许补买少量

- 可以推荐库存高度匹配但缺少少量食材的结果；
- 结果展示库存匹配程度；
- 缺少项区分关键食材、普通配料和可选配料；
- 第一版只展示缺少项，不提供完整的可勾选买菜清单；
- “少量”的默认阈值属于上线前可配置参数，需通过测试数据调整。

#### 库存数量满足状态

系统需要按菜谱所需份数，将每种必需食材的所需数量与所有可用库存批次的合计数量进行比较，并展示以下状态：

- **满足**：库存数量和单位信息足以完成可靠比较，合计可用量不低于菜谱所需量；已启用且默认不维护精确余量的常备基础项，也可记为“满足（按常备项假设）”；
- **可能满足**：库存为约数，或数量恰好接近所需量，系统判断大概率可用但无法作确定承诺；
- **不足**：不存在该食材的可用库存，或可靠换算后的合计数量明确低于所需量；
- **未知**：存在该食材记录，但缺少数量、单位不可比较，或现有信息不足以判断是否达到所需量。

计算与展示规则：

- 同一食材存在多个批次时，先在可靠换算的前提下汇总可用量；
- 一顿菜单中的多道菜使用同一种食材时，应先汇总整顿菜单的需求量，不能分别重复认定为满足；
- 单位无法可靠换算时不得强行换算，状态应为“未知”；
- 可选配料不影响整道菜或菜单的总体满足状态，但仍可单独展示；
- 整体状态按必需食材汇总：存在任一“不足”则整体为“不足”；不存在“不足”但存在“未知”则整体为“未知”；仅存在“满足”和“可能满足”且至少一项为“可能满足”则整体为“可能满足”；全部必需食材均满足时整体为“满足”；
- 推荐卡片和菜谱详情需同时展示整体状态，并允许用户查看各食材的状态与判断依据；
- “可能满足”和“未知”不能以确定性文案描述为“可以直接制作”。

### 10.7 菜谱详情

系统菜谱至少包括：

- 菜名；
- 默认份数；
- 菜品分类与菜系；
- 主食、主菜、配菜等餐食角色；
- 辣度、荤素、烹饪方式与预计时间；
- 所需食材、用量、单位及其重要程度；
- 可替代食材关系；
- 分步骤烹饪说明；
- 过敏原及饮食限制标签；
- 可用时提供热量和蛋白质估算；
- 内容来源、审核状态和版本信息。

### 10.8 确认制作与库存扣减

- 查看菜谱不更新库存；
- 用户点击“我做了这顿饭”后才进入扣减流程；
- 系统按实际选择的份数生成预计消耗；
- 扣减预览需列出食材、批次和预计数量；
- 多批次时优先建议消耗临期批次；
- 用户可以修改数量、取消某项或跳过扣减；
- 用户最终确认后统一更新，不能留下部分成功、部分失败的状态；
- 模糊库存扣减后仍以“约剩余”表达；
- 数量减至零时提示将食材标记为已用完。

### 10.9 随机菜单

- 不要求使用库存；
- 使用统一偏好、硬约束、人数和营养规则；
- 输出具体菜品组合、对应菜谱和按人数调整的食材用量；
- 第一版不生成完整买菜清单；
- 随机菜单结果不会自动写入库存或扣减库存。

### 10.10 饮食偏好与限制

统一偏好系统至少支持：

- 过敏原；
- 明确不吃的食材；
- 荤食、素食等饮食类型；
- 喜欢和不喜欢的菜品；
- 辣度；
- 菜系偏好；
- 居家补买预算、随机菜单食材采购总预算、外出人均预算，以及各自是否严格；
- 烹饪时间偏好；
- 均衡、减脂、增肌等宽泛营养目标；
- 可选每餐热量与蛋白质目标区间。

偏好分为：

- 硬约束：过敏原、明确不吃、饮食禁忌等；
- 软偏好：菜系、辣度、喜欢程度、烹饪时间、近期是否吃过等；
- 目标区间：场景对应的预算、热量和蛋白质目标；
- 可选严格条件：用户可以将当前场景的预算设为不能超过的硬上限。

无结果时，产品只能建议用户主动放宽软偏好或目标区间，不能突破硬约束。

#### 预算语义

预算必须随场景使用不同名称、计算口径和提示，不得共用一个含义模糊的“预算”字段：

- **居家补买预算**：用于库存推荐的“允许补买少量”模式，表示为了完成本次整顿菜单预计还需购买食材的总金额；不包含现有库存的历史购买价值，也不表示每人预算；
- **随机菜单食材采购总预算**：用于不参考库存的随机菜单，表示为本次整顿菜单采购食材的预计总金额；
- **外出人均预算**：用于菜系和真实餐厅推荐，表示每位用餐者预计可接受的消费金额；界面可根据人数辅助展示预计总支出，但筛选口径仍以人均预算为准。

预算规则：

- 所有金额以人民币展示；
- 三类预算分别保存和使用，切换场景时不得把上一个场景的数值无提示地套用到当前场景；
- 预算默认为目标区间，用户可主动设置为严格上限；
- 居家补买预算只计算缺少且需要购买的食材；可选配料除非用户选择购买，否则不计入；
- 食材采购金额属于区域和时间相关的估算值，需展示估算属性和可用的数据来源时间；
- 外出人均预算基于可靠的人均消费数据；它不等同于最终账单，也不承诺服务费、酒水或临时价格变动后的实际支出；
- 在严格预算模式下，无法可靠估算补买金额或缺少可靠人均消费数据的候选不得被描述为符合预算，应被排除或单独提示用户放宽严格条件；
- 在非严格模式下，预算数据缺失的候选可以保留，但必须标记“预算未知”，且不得因缺失数据获得更高推荐权重。

### 10.11 营养功能

- 居家菜单默认考虑主食、蛋白质来源和蔬菜搭配；
- 支持均衡、减脂、增肌等宽泛目标；
- 用户可选设置每餐热量和蛋白质区间；
- 展示值必须标记为估算值，并说明估算受食材品牌、实际用量、油盐和烹饪损耗影响；
- 用户自定义菜谱缺少营养数据时显示“暂无营养估算”；
- 第一版不提供每日摄入累计、饮食打卡、诊断、治疗建议或医疗级精度；
- 外出推荐不在缺少具体菜品数据时展示热量或蛋白质。

### 10.12 收藏、反馈与屏蔽

- “再来一次”：仅更换本次结果，不改变长期偏好；
- “今天不想吃”：在短期内降低重复概率；
- “不喜欢”：长期降低推荐权重；
- “不再推荐”：加入明确屏蔽，用户可在设置中解除；
- “收藏”：提高推荐权重，但不保证频繁出现；
- 用户可以报告菜谱错误、餐厅信息错误或不恰当的 AI 建议。

### 10.13 基础自定义菜谱

- 用户可创建仅本人可见的私有菜谱；
- 必填内容包括菜名、默认份数、食材及大致用量、步骤；
- 可选填写分类、菜系、辣度和烹饪时间；
- 保存后可以参与该用户的库存匹配和随机推荐；
- 不强制填写营养数据；
- 系统菜谱与用户菜谱采用兼容的业务结构，但必须保留内容来源、所有者、可见范围和数据完整度；
- 第一版不包含公开发布、评论、他人收藏、举报和公开内容审核。

### 10.14 受限制的 AI 辅助

第一版实际开放 AI 辅助，但仅限以下用途：

- 用户主动请求缺少食材的替代建议；
- 根据人数和库存解释用量调整；
- 对原有结构化步骤进行更易理解的说明；
- 提供不修改原菜谱的临时调整建议。

限制：

- AI 不自由生成完整菜谱并直接进入正式推荐池；
- AI 输出必须重新经过硬约束检查；
- AI 建议与原始结构化菜谱分开显示并明确标识；
- AI 不能自行修改系统菜谱、用户菜谱、库存或偏好；
- 用户确认前，AI 建议不产生任何数据变更；
- AI 不可用时提供明确提示，核心功能继续可用。

### 10.15 外出菜系抽取

- 支持只抽菜系或餐饮类别；
- 可根据人数、外出人均预算、辣度、荤素和菜系偏好过滤；
- 支持用户已决定菜系后直接进入餐厅抽取；
- 结果需要能够继续衔接真实餐厅推荐。

### 10.16 真实餐厅推荐

- 首版只覆盖深圳市；
- 支持当前位置和指定区域/商圈两种范围；
- 支持直接抽餐厅或先选菜系再抽餐厅；
- 真实餐厅必须具备名称、餐饮类别、可定位地址或坐标、距离、数据来源和最近获取时间；
- 营业时间、营业状态、人均消费、评分、评价数量、电话、图片和外卖标识在数据可靠时展示，缺失时明确说明；
- 缺少地址或无法确认位置的餐厅不得进入推荐池；
- 营业状态未知不得显示为正在营业；
- 可以跳转到外部地图查看路线，第一版不自行提供导航；
- 第一版不推荐餐厅内具体菜品；
- 第一版不提供外卖配送范围、配送费、起送价、实时价格或下单；
- 不接受付费置顶或商业排序。

### 10.17 位置权限与常用地点

- “附近就餐”由用户主动授权当前位置；
- 拒绝或定位失败时支持手动选择；
- “指定区域”不要求定位权限；
- 精确位置只用于当次搜索，第一版不保存位置轨迹；
- 用户可主动收藏常用地点；
- 不进行后台持续定位；
- 向第三方服务发送位置前说明用途。

### 10.18 账户与云端同步

- 游客可完整体验首要流程；
- 登录使用手机号短信验证码，不设置密码；
- 登录后数据云端保存并支持跨设备使用；
- 首次登录需要处理本地与云端数据合并；
- 不得静默覆盖已有库存或自定义菜谱；
- 合并失败时保留原始数据并允许重试；
- 退出登录后不得继续向后续设备使用者展示原账号数据；
- 用户可以注销账户和删除个人数据；
- 第一版不支持微信登录，但未来允许微信身份与手机号账户绑定。

### 10.19 内部内容管理

最低能力包括：

- 系统菜谱的新增、编辑、审核、发布、下线和版本记录；
- 标准食材、别名、分类、单位、替代关系和过敏原的维护；
- 营养估算来源和修订记录的维护；
- 用户主动反馈的查看和处理状态记录；
- 错误、停业或不适合推荐餐厅的内部屏蔽；
- 内部人员权限和关键操作记录。

内部管理能力不等于面向用户的内容社区。

---

## 11. 随机推荐机制

### 11.1 总体流程

1. 获取本次场景、人数、餐次和用户主动设置的条件。
2. 应用过敏原、明确不吃、饮食禁忌和当前场景的严格预算等硬约束。
3. 居家场景计算各食材及整顿菜单的库存数量满足状态、临期优先级和缺少项。
4. 生成单菜或完整菜单候选，并检查基础营养搭配。
5. 根据软偏好、历史反馈、近期重复和内容质量形成相对优先级。
6. 从达到最低标准的候选中加权随机抽取。
7. 展示结果、匹配程度和主要推荐理由。

### 11.2 居家候选因素

- 各食材及整顿菜单的库存数量满足状态；
- 库存关键食材匹配程度；
- 临期或“尽快食用”食材利用程度；
- 是否符合严格库存/少量补买模式；
- 缺少的关键食材和普通配料数量；
- 补买食材的预计总金额及其是否符合居家补买预算；
- 人数与份量适配；
- 菜单内菜品角色和营养搭配；
- 烹饪时间与难度偏好；
- 收藏、喜欢、不喜欢、短期回避和永久屏蔽；
- 近期是否已推荐或已制作。

### 11.3 外出候选因素

- 是否位于用户选择范围；
- 菜系或餐饮类型匹配；
- 外出人均预算及其是否为严格上限；
- 距离；
- 营业信息及其可靠程度；
- 数据新鲜度和完整度；
- 用户历史反馈和短期重复；
- 内部屏蔽状态。

### 11.4 随机性要求

- 不固定返回唯一最高分结果；
- 明显低质量或低匹配结果不能仅因随机而入选；
- 连续重抽应降低已出现结果的短期权重；
- 候选太少时说明原因，不制造虚假多样性；
- 推荐理由应来自真实规则或数据，不得生成与实际评分无关的解释。

---

## 12. 食材库存机制

### 12.1 标准化与别名

- 用户输入需尽可能映射到标准食材；
- 保留用户原始输入，便于纠错；
- 支持常见中文别名和地区称呼；
- 无法识别时允许用户保留自定义食材，但其推荐匹配能力可能受限；
- 食材标准化错误需允许用户更正。

### 12.2 单位与数量

- 面向中国大陆用户，支持克、千克、斤、毫升、升、个、只、颗、把、袋、盒等常用单位；
- 系统只在换算关系可靠时进行自动换算；
- “少量”“半盒”“约一把”等模糊量可以保存，但不得伪装为精确克数；
- 推荐和扣减应允许用户手动修正换算结果。

### 12.3 批次

- 同一标准食材可存在多个库存批次；
- 合并批次前需要用户确认；
- 预计过期时间不同的批次不应被无提示合并；
- 推荐消耗时优先建议临期批次；
- 删除或用完批次需要有明确反馈，误操作应提供合理恢复路径。

### 12.4 库存可信度

- 系统应显示数据是否为精确量或约数；
- 长期未更新的库存可提示用户确认，但不能擅自删除；
- 推荐结果需允许用户快速指出“其实没有了”或“数量不够”；
- 此类反馈只在用户确认后更新库存。

---

## 13. 菜谱与内容机制

### 13.1 首版内容定位

- 以中国大陆常见中式家常菜为主；
- 补充食材易获得、制作门槛较低的西式简餐；
- 优先覆盖深圳核心用户常见的肉蛋奶、叶菜、根茎、豆制品和米面等库存；
- 不追求覆盖全部八大菜系、复杂西餐、专业烘焙、小众食材和宴席菜；
- 优先保证结构完整、匹配准确和安全标签可靠，而不是单纯追求数量。

### 13.2 内容来源

- 可由内部原创整理和取得合法使用权的数据共同组成；
- 菜谱文本、图片、营养和第三方数据均需明确使用权和来源；
- 未完成结构化或基本审核的内容不得进入正式推荐池；
- 任何外部来源数据都需要保留来源与版本信息。

### 13.3 系统与用户内容兼容

- 两类菜谱共享核心字段，使其都能参与库存匹配；
- 系统菜谱具备审核、版本和发布状态；
- 用户菜谱具备所有者、私有可见性和完整度状态；
- 用户编辑自己的菜谱不会修改系统菜谱；
- 用户菜谱的错误只影响其本人推荐池。

---

## 14. 外出推荐机制

### 14.1 范围

- 数据范围限定深圳市；
- 支持当前附近、指定区域、商圈和手动地点；
- 允许用户收藏公司附近等常用地点；
- 居家功能不因用户不在深圳而被限制。

### 14.2 餐厅数据最低要求

必须具备：

- 名称；
- 餐饮类别或菜系；
- 地址或坐标；
- 与选择位置的距离；
- 数据来源；
- 最近获取时间。

可选展示：

- 营业时间与当前营业状态；
- 人均消费；
- 评分与评价数量；
- 联系电话；
- 图片；
- 外卖支持标识。

### 14.3 数据异常

- 第三方服务不可用时显示失败状态和重试入口；
- 不使用明显过期的数据假装实时推荐；
- 营业状态未知时明确标记未知；
- 用户可以报告地址错误、停业或分类不准确；
- 内部人员可以临时屏蔽问题餐厅；
- 外部服务恢复后允许重新获取结果。

### 14.4 能力边界

第一版不承诺：

- 餐厅完整实时菜单；
- 店内具体菜品推荐；
- 外卖配送范围、配送费、起送价和实时价格；
- 直接下单；
- 排队、订座；
- 产品内导航；
- 餐厅过敏原零风险保证。

用户有过敏或严重饮食禁忌时，产品需提醒其向餐厅确认原料和制作过程中的交叉接触风险。

---

## 15. 数据需求

### 15.1 用户与账户数据

- 手机号身份与验证状态；
- 游客本地身份和登录后的合并状态；
- 用户偏好与硬约束；
- 收藏、短期回避、长期降权和永久屏蔽；
- 常用地点；
- 数据创建、更新和删除状态。

### 15.2 食材主数据

- 标准名称；
- 中文别名；
- 分类；
- 可用单位及可靠换算关系；
- 过敏原关联；
- 常备项属性；
- 可替代关系；
- 可用时提供深圳地区食材价格估算、价格区间、来源和更新时间；
- 数据来源和审核状态。

### 15.3 菜谱数据

- 菜谱基础信息；
- 份数与菜单角色；
- 食材、用量、单位、重要程度和替代关系；
- 烹饪步骤；
- 菜系、辣度、荤素、时间和难度标签；
- 过敏原和饮食限制；
- 营养估算及来源；
- 系统/用户来源、所有者、可见范围、审核和版本状态。

### 15.4 库存数据

- 用户或游客所有者；
- 标准食材与原始输入；
- 批次；
- 数量、单位及精确度类型；
- 购买/录入时间；
- 预计过期时间；
- 尽快食用标记；
- 状态和修改记录。

### 15.5 推荐数据

- 场景和时间；
- 人数、餐次和结果形式；
- 当次硬约束、软偏好和目标区间；
- 当次预算类型、金额区间、严格性、估算结果及数据完整度；
- 各必需食材及整道菜/整顿菜单相对于本次所需份数的数量满足状态与判断依据；
- 库存模式及候选集形成原因；
- 最终结果和推荐理由；
- 重抽、接受、制作、扣减和反馈；
- AI 是否被调用及其必要审计状态。

数据分析应尽量使用匿名或去标识方式，避免为统计目的长期保存不必要的完整敏感输入。

### 15.6 餐厅和位置数据

- 餐厅基础信息、类别、坐标、来源和更新时间；
- 可选营业、消费、评分、图片和外卖信息；
- 本次搜索中心点、范围和区域标识；
- 用户主动收藏的常用地点；
- 餐厅内部屏蔽和错误反馈。

精确位置只用于当次搜索，不形成位置轨迹。

---

## 16. 第三方数据与服务需求

第一版可能需要以下类别的外部能力，但本阶段不选择供应商：

- 手机号短信验证码服务；
- 定位、地址解析、区域/商圈、餐厅 POI 和地图跳转能力；
- 深圳地区食材价格或可用于形成采购金额估算的数据；
- 结构化营养数据；
- 受限制的 AI 辅助服务；
- 合法授权的菜谱或图片内容来源。

第三方能力需满足：

- 在中国大陆和深圳的可用性；
- 数据使用、展示和缓存授权清晰；
- 数据来源和更新时间可追踪；
- 位置、手机号、饮食限制等个人信息只按必要范围处理；
- 服务不可用时产品具备明确降级路径；
- AI 服务不得使用请求数据训练其通用模型，该要求需在后续协议和服务选择中落实。

---

## 17. 隐私与数据保护

### 17.1 基本原则

- 最少收集；
- 用途明确；
- 用户主动授权；
- 数据可查看、可更正、可删除；
- 游客与登录用户的数据边界清晰；
- 第三方数据传输范围可说明。

### 17.2 位置数据

- 仅用户主动使用附近推荐时请求；
- 拒绝授权不影响指定区域模式；
- 第一版不保存位置轨迹；
- 不进行后台持续定位；
- 常用地点由用户主动收藏和删除。

### 17.3 AI 数据最小化

- 仅用户主动调用时发送必要的食材、人数、硬约束和相关菜谱内容；
- 不发送手机号、账户身份、精确位置或完整使用历史；
- 外出定位数据不用于 AI 菜谱功能；
- AI 日志使用内部匿名标识，并设置明确保留期限；
- 账户删除时删除可关联的 AI 使用记录；
- 因安全审计或法规必须保留的例外需向用户说明。

### 17.4 账户与删除

- 用户可以退出登录；
- 用户可以注销账户并删除库存、偏好、收藏、屏蔽和自定义菜谱；
- 删除流程需说明影响和不可恢复内容；
- 退出登录后清理不应继续显示的账号数据；
- 游客应被告知本地数据可能因浏览器清理而丢失。

---

## 18. 异常情况与恢复

### 18.1 库存为空

- 引导快速添加常见食材；
- 允许跳过并使用随机菜单；
- 不展示空白页或无解释的失败。

### 18.2 无符合条件的菜谱

- 说明主要冲突原因；
- 不突破硬约束；
- 建议补充库存、切换少量补买、调整人数或主动放宽某项软偏好。

### 18.3 库存数量不足或已失真

- 允许在结果页快速标记“没有了”或“数量不足”；
- 用户确认后再更新库存；
- 返回推荐时重新计算候选。

### 18.4 自定义食材无法识别

- 允许保存原始名称；
- 告知匹配能力可能受限；
- 提供选择标准食材或反馈别名的路径。

### 18.5 菜谱营养数据缺失

- 显示“暂无营养估算”；
- 不由 AI 凭空生成看似精确的数值。

### 18.6 预算数据缺失或无法可靠估算

- 居家场景说明哪些缺少食材没有可靠价格，补买总金额标记为“估算不完整”；
- 外出场景缺少可靠人均消费数据时标记“预算未知”；
- 严格预算模式不得把这些候选显示为符合预算，并应提供取消严格预算或调整条件的入口；
- 非严格模式可以保留候选，但预算未知不得被包装成低价或高性价比。

### 18.7 AI 服务失败或结果不安全

- 保留原始结构化菜谱；
- 显示重试或暂不可用状态；
- 不自动采用 AI 结果；
- 检测到违反硬约束时阻止展示为可采用建议，并允许报告问题。

### 18.8 定位被拒绝或失败

- 允许重新授权；
- 支持手动选择深圳区域、商圈或地点；
- 不反复强迫请求权限。

### 18.9 餐厅数据不足或第三方异常

- 明确区分无结果和服务异常；
- 提供调整范围、条件或重试的方式；
- 不使用未知营业信息冒充实时状态。

### 18.10 短信验证码失败

- 游客本地数据保持不变；
- 提供清晰错误、合理重试和重新发送入口；
- 避免重复提交造成多个互相冲突的登录状态。

### 18.11 登录数据合并失败

- 本地和云端原数据均不得被破坏；
- 明确告知哪些数据尚未合并；
- 允许用户稍后重试。

### 18.12 库存扣减中断

- 未最终确认时不更新库存；
- 更新应整体成功或整体不生效；
- 失败后保留用户已编辑的扣减预览，便于重试。

---

## 19. UX 与可访问性要求

- 手机端优先，同时保证桌面浏览器可完成全部核心流程；
- 首次使用采用渐进式设置，避免一次性填写完整档案；
- 每个核心页面保持一个明确主操作；
- 主要触控操作需具有足够的点击区域和明确按下反馈；
- 盲盒动画应短、可中断，不阻塞用户继续操作；
- 支持减少动态效果的系统偏好，并提供静态结果体验；
- 不能只依靠颜色表达临期、错误、成功或屏蔽状态；
- 表单字段具有可见标签、就近错误提示和恢复方式；
- 定位、短信、AI 和餐厅数据加载均需提供加载、失败、空状态和重试反馈；
- 页面缩放、键盘操作、屏幕阅读器语义和文字可读性应作为公开测试的基本可用要求；
- 桌面端不简单拉伸手机布局，应控制内容宽度并保持清晰层级。

视觉基调保持温暖、简洁、有生活感且不过度卡通。暖黄色、奶油色和米白色可作为后续设计探索方向，但本 PRD 不确定具体色值、字体、组件样式或动画方案。

---

## 20. MVP 范围

第一版必须包含：

- 手机优先网页及桌面兼容；
- 游客模式；
- 手机号短信验证码登录；
- 游客数据与云端数据合并；
- 个人库存的轻量录入和批次化保存；
- 常备基础项；
- 应用内临期提醒；
- 午餐、晚餐和简餐/正餐选择；
- 按人数自适应的一道菜或菜单推荐；
- 严格库存与允许少量补买模式；
- 满足、可能满足、不足、未知四种库存数量满足状态；
- 居家补买预算、随机菜单食材采购总预算和外出人均预算；
- 结构化系统菜谱及按人数调整的用量；
- 硬约束、软偏好和目标区间；
- 受控加权随机及可解释推荐理由；
- 收藏、短期回避、不喜欢和永久屏蔽；
- 确认制作、扣减预览和库存更新；
- 随机菜单模式；
- 基础营养搭配、热量和蛋白质估算；
- 私有基础自定义菜谱；
- 受限制且由用户主动触发的 AI 辅助；
- 深圳市菜系抽取和真实餐厅推荐；
- 附近定位、指定区域、手动选点和常用地点；
- 外部地图路线跳转；
- 账户注销与个人数据删除；
- 最低限度的内部内容管理能力；
- 必要的加载、空状态、错误和恢复流程。

---

## 21. 非 MVP 功能

第一版不包含：

- 拍照识别食材；
- 自动推断食材真实保质期或是否变质；
- 家庭共享库存、成员邀请和协作权限；
- 浏览器后台推送、短信临期提醒或微信提醒；
- 完整买菜清单；
- 每日营养累计、饮食打卡或医疗膳食管理；
- AI 自由生成菜谱并直接进入正式推荐池；
- 用户公开发布菜谱、社区、评论、关注和公开审核；
- 餐厅内具体菜品推荐；
- 餐厅完整实时菜单；
- 外卖配送、实时价格、下单；
- 排队、订座和产品内导航；
- 深圳以外的真实餐厅推荐；
- 微信小程序和微信登录；
- 付费订阅、广告、赞助餐厅或商业排序；
- 早餐、下午茶、夜宵、宴请和节庆菜单；
- 专业烘焙、复杂西餐及全面地域菜系覆盖。

---

## 22. 后续扩展方向

- 食材拍照识别和识别结果确认；
- 基于可靠规则的保质期辅助填写；
- 完整买菜清单、合并同类项和采购完成后入库；
- 家庭空间、共享库存和成员权限；
- 浏览器推送、微信提醒和小程序；
- 微信身份与手机号账号绑定；
- 更多城市和全国外出推荐；
- 在具备合法可靠数据时提供餐厅菜单和店内菜品推荐；
- 与外卖、订座或导航服务进行明确授权的跳转或合作；
- 更丰富的营养指标及饮食记录；
- 用户公开菜谱和相应内容治理体系；
- 更丰富的菜系、西餐、早餐和节庆菜单；
- 基于真实反馈优化个性化权重，但仍保持可解释和可控随机；
- 在明确标识和不损害自然推荐可信度的前提下探索商业模式。

---

## 23. 验收标准

### 23.1 首次体验

- 游客无需登录即可选择居家或外出场景；
- 新用户能够通过少量食材完成一次库存推荐；
- 用户能够跳过库存并使用随机菜单；
- 硬性饮食限制在首次核心流程中可被设置和修改。

### 23.2 库存

- 用户可以只填名称创建库存，也可以填写数量、单位和日期；
- 同一食材支持多个批次，并能选择合并或分别保存；
- 临期状态只基于明确日期或用户标记；
- 常备基础项可编辑且不会覆盖硬约束；
- 模糊数量在展示、推荐和扣减中保持约数语义。
- 每种必需食材及整顿菜单均能得到“满足 / 可能满足 / 不足 / 未知”之一，并能查看判断依据；
- 多批次和菜单内重复使用的食材按可靠换算后的合计需求与合计库存判断；
- 单位不可比较或数量缺失时不会被错误标记为满足。

### 23.3 居家推荐

- 推荐结果符合所有已设置硬约束；
- 严格库存和允许少量补买产生符合各自定义的候选结果；
- 少量补买结果明确展示缺少项及其重要程度；
- 严格库存模式不会把明确“不足”的结果当作无需补买推荐；
- “可能满足”与“未知”具有明确提示，不会被描述为确定可制作；
- 居家库存推荐使用补买总预算，不把现有库存价值计入补买金额；
- 随机菜单使用整顿菜单的食材采购总预算；
- 结果按人数调整菜品组合和食材用量；
- 结果展示至少一个真实可追溯的推荐理由；
- 无结果时不突破硬约束，并提供可执行的恢复方式。

### 23.4 随机体验与反馈

- 随机只发生在合格候选中；
- 连续重抽不会无解释地反复返回同一结果；
- “再来一次”“今天不想吃”“不喜欢”“不再推荐”和收藏产生符合定义的不同影响；
- 用户可以查看并解除永久屏蔽。

### 23.5 制作与扣减

- 查看菜谱不会扣减库存；
- “我做了这顿饭”能生成扣减预览；
- 用户可以修改、取消或跳过具体扣减项；
- 最终确认后库存统一更新；
- 中断或失败不会造成部分库存被错误扣除。

### 23.6 菜谱与营养

- 系统推荐池中的菜谱具有结构化食材、用量、步骤和必要安全标签；
- 营养数值明确标记为估算；
- 无营养数据时显示缺失，不生成虚假精确值；
- 用户可以创建私有基础菜谱并使其参与个人推荐。

### 23.7 AI 辅助

- AI 仅在用户主动操作后调用；
- AI 建议与原始菜谱明确区分；
- AI 输出违反硬约束时不能作为可采用建议呈现；
- AI 不能未经确认修改库存、偏好或菜谱；
- AI 服务不可用时，库存推荐和菜谱查看仍可正常使用；
- 发送给 AI 的数据不包含手机号、精确位置和完整账户历史。

### 23.8 外出推荐

- 用户可以使用当前位置或手动地点进行深圳餐厅推荐；
- 拒绝定位后仍可使用指定区域模式；
- 推荐餐厅具有最低必需数据，并能定位和查看来源时间；
- 外出预算以人均金额筛选，并与居家补买预算分别保存和展示；
- 严格人均预算下，缺少可靠人均消费数据的餐厅不会被标记为符合预算；
- 缺失营业、消费、评分或外卖数据时明确显示未知；
- 用户可以跳转外部地图查看路线；
- 不展示餐厅内具体菜品、配送承诺或虚构实时信息。

### 23.9 账户与隐私

- 游客可完成核心功能；
- 手机号验证码登录无需密码；
- 本地与云端存在数据时，合并流程不会静默覆盖；
- 退出登录后不继续展示原账号私有数据；
- 用户可以注销账户并删除个人数据；
- 第一版不形成位置轨迹；
- 第三方数据用途和必要授权具有清晰说明。

### 23.10 内部内容管理

- 授权人员能够发布、修正和下线系统菜谱；
- 能够维护食材别名、单位和过敏原等关键主数据；
- 能够处理用户主动反馈并屏蔽明显错误餐厅；
- 关键内容修改具有人员和时间记录；
- 未授权用户无法访问内部管理能力。

### 23.11 基础 UX

- 手机和桌面浏览器均可完成所有核心流程；
- 核心操作具有明确加载、成功、失败、空状态和恢复反馈；
- 动画可中断，并支持减少动态效果；
- 不只通过颜色表达重要状态；
- 表单和交互控件具备清晰标签、键盘可用性和基本屏幕阅读器语义。

---

## 24. 上线前需配置但不影响总体架构的参数

以下参数需在内容准备、可用性测试或第三方数据评估后确定，不在本 PRD 中凭空固定：

- 首批系统菜谱数量和中西餐比例；
- 默认临期提前天数；
- “允许补买少量”的默认阈值；
- 深圳食材价格估算的数据来源、更新时间和可接受区间；
- 居家补买预算、随机菜单采购预算和外出人均预算的默认区间；
- 不同人数下默认菜品数量；
- 营养目标的默认区间；
- 外出推荐默认距离和最大范围；
- 餐厅数据可接受的新鲜度范围；
- 短期回避和近期重复的时间窗口；
- 短信验证码发送频率与风控规则；
- 匿名日志、AI 审计记录和已删除账户必要记录的保留期限；
- 小范围公开测试的用户规模和邀请方式。

这些参数确定后，应补充进上线配置和测试用例，但不得改变本 PRD 已定义的硬约束、安全边界和 MVP 范围。

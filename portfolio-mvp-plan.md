# 「今天吃什么」Portfolio MVP 快速交付计划

## 1. 交付结论

- **范围**：只交付“首页 → 库存 → 条件 → 盲盒 → 推荐 → 菜谱 → 制作确认 → 扣减 → 更新后首页”的居家闭环。
- **实现策略**：保留 `prototype/` 作为已验收参考；在项目根目录建立公开 Demo 入口，复用其交互与组件，不重做已验证流程。
- **最小技术方案**：原生 HTML + CSS + JavaScript ES Modules + LocalStorage + 静态 Mock 菜谱数据；无后端、无运行时依赖、无 secret。
- **框架决定**：不迁移 React/Vite。当前运行时代码仅约 1,296 行，原生结构足以支撑本次范围；迁移会增加重写、构建和回归成本，却不能直接解决数据持久化与推荐规则缺口。
- **部署建议**：GitHub Pages + GitHub Actions。它能直接发布静态文件，适合公开仓库和 Portfolio 展示；官方支持无构建步骤的 Pages 工作流。[GitHub Pages 官方说明](https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages)

## 2. 现有 prototype 复用评估

| 部分 | 复用判断 | 处理方式 |
|---|---|---|
| 页面层级与核心流程 | 约 90% 可复用 | 保持流程、CTA、Sheet、返回和异常恢复语义 |
| CSS 组件与响应式基础 | 约 65–75% 可复用 | 保留布局、触控、焦点、Sheet、四态和动效结构；替换视觉 Token 并精修组件 |
| JavaScript 渲染与交互 | 约 50–60% 可复用 | 保留路由、焦点、盲盒、未保存保护和扣减视图；将固定 Frame 状态改为真实数据驱动 |
| 自动化验收思路 | 约 60% 可复用 | 保留核心流程断言，移除 Frame/主持人依赖并修正本机绝对路径 |
| 测试主持人和 Frame 元数据 | 不进入产品 | 从公开入口删除；`prototype/` 中继续保留作为设计过程资产 |

按实际运行时代码估算，**约 55–65% 可以在整理后直接保留，约 75% 的交互与 UX 资产可以继承**。主要新增工作不是换框架，而是数据模型、LocalStorage、真实推荐规则和 Portfolio 视觉。

## 3. 预计主要文件

- 保留：`prototype/`，作为低保真参考与原 76/76 验收资产。
- 创建：`index.html`、`src/styles.css`、`src/app.js`。
- 创建：`src/data/recipes.js`、`src/storage.js`、`src/recommender.js`。
- 创建或整理：`tests/portfolio-mvp.spec.mjs`、`package.json`。
- 创建：`README.md`、`LICENSE`、`.gitignore`、`.github/workflows/pages.yml`。
- 创建：`assets/`，存放经过压缩的本地视觉资源和 README 截图。

## 4. 开发步骤

### 步骤 1：建立公开 Demo Shell

- **状态**：已完成
- **目标**：建立面向普通用户的根目录入口，并复用原型的页面骨架、返回、Sheet 和核心导航。
- **修改范围**：创建 `index.html`、`src/app.js`、`src/styles.css`；移除公开 UI 中的 Frame ID、主持人面板、测试任务、验证问题和 Deferred 占位；`prototype/` 不修改。
- **验收方式**：根目录页面可直接通过静态服务器打开；主界面无原型调试信息；首页、库存、推荐条件、结果、菜谱和扣减页面均有明确入口；原型原 76/76 基线仍可独立运行。
- **实际完成**：创建根目录公开入口、独立样式、Hash 页面流和基础浏览器验收；公开界面不含主持人、Frame、任务或验证问题。
- **测试结果**：公开 Shell 10/10；原 `prototype/verify.mjs` 76/76；原型四文件哈希未变化。
- **计划偏差**：无。测试首次发现 favicon 404，补充内联图标后通过。

### 步骤 2：建立本地数据与持久化

- **状态**：已完成
- **目标**：让库存、条件、最近结果和制作状态在刷新后仍然存在，并能安全恢复。
- **修改范围**：创建 `src/storage.js`；定义版本化 LocalStorage 单文档结构、首次 Sample Data、数据校验、迁移/重置和写入失败处理；不保存身份、位置或敏感信息。
- **验收方式**：添加或修改库存后刷新仍保留；损坏或旧版本数据可回退/迁移；LocalStorage 写入失败不会破坏内存中的制作前库存；可一键恢复演示数据。
- **实际完成**：建立 V1 单文档数据、演示库存、校验、V0 迁移、损坏恢复、不可变快照、写入失败回滚与重置；公开应用已读取本地库存和最近结果。
- **测试结果**：存储单元测试 5/5；浏览器累计验收 12/12。
- **计划偏差**：无。

### 步骤 3：完成可维护库存与用餐条件

- **状态**：已完成
- **目标**：把原型固定库存改为用户可操作的数据，并让人数和基础条件真正生效。
- **修改范围**：在 `src/app.js` 实现库存列表、手动添加、编辑、数量精度、单位、临期/未知提示；实现人数、餐次、结果形式、库存模式、菜系/荤素/辣度/时间等基础条件。
- **验收方式**：用户可只填名称，也可填写数量、单位和日期；同食材可保存批次；1/2/4 人切换会影响后续需求量；刷新后数据和最近条件一致；键盘、返回和未保存保护可用。
- **实际完成**：库存支持仅名称录入、数量/单位/精度/日期/临期标记、同名批次追加和首批编辑；推荐条件支持人数、餐次、结果形式、库存模式、菜系、荤素、辣度和时间，并持久化最近选择。
- **测试结果**：存储单元测试 5/5；浏览器累计验收 19/19，覆盖批次、刷新、焦点、Escape 和未保存保护。
- **计划偏差**：无。首次焦点检查暴露点击默认焦点与延迟转移的竞态，改为同步聚焦并在 32ms 后补焦；测试同步等待可见焦点状态。

### 步骤 4：接入 15–30 道菜谱与透明推荐规则

- **状态**：已完成
- **目标**：以少量高质量菜谱支撑真实可运行的盲盒推荐，而不是固定跳转。
- **修改范围**：创建 `src/data/recipes.js` 与 `src/recommender.js`；准备约 18–24 道代表性中西餐；实现“条件过滤 → 按人数计算需求 → 库存四态匹配 → 候选集合 → 加权随机”，并降低连续重复概率。
- **验收方式**：同一库存和条件能产生合格候选；不符合硬条件的菜谱不会入选；满足/可能满足/不足/未知均可由真实数据计算；无结果有恢复路径；推荐理由能追溯到库存、临期或条件规则；界面不宣称 AI 推荐。
- **实际完成**：接入 20 道中西式结构化菜谱；实现条件过滤、人数缩放、四态库存匹配、补买预算、临期加权、近期结果降权和随机抽取；结果页展示真实状态、理由与 Mock 补买估价，无结果页可返回条件或库存。
- **测试结果**：推荐规则单元测试 15/15；存储测试 5/5；浏览器累计验收 21/21，含无结果恢复；JavaScript 语法检查通过。
- **计划偏差**：无。补买预算字段仅在“允许少量补买”时显示，并明确它不是外出人均预算。

### 步骤 5：打通菜谱、制作确认与原子扣减

- **状态**：已完成
- **目标**：完成 Portfolio MVP 的核心价值闭环，并保证失败不产生部分扣减。
- **修改范围**：数据驱动菜谱、按人数调整用量、库存状态明细、制作确认、可编辑批次扣减、成功/失败与更新后首页；LocalStorage 采用“校验副本 → 单次持久化 → 成功后替换内存状态”的提交方式。
- **验收方式**：查看菜谱不扣库存；“我做了这顿饭”只进入预览；用户可修改或跳过单项；确认后所有项一起更新；模拟写入失败时库存保持原值、编辑完整保留并可重试；更新后首页与库存页数值一致。
- **实际完成**：菜谱、需求量和营养估算均由所选菜谱及人数驱动；制作确认生成按临期/日期优先的批次预览，支持修改和跳过单项；提交采用校验副本后单次写入，成功后同步首页，失败页保留修改并可重试。
- **测试结果**：扣减单元测试 10/10；推荐 15/15；存储 5/5；浏览器累计验收 29/29，覆盖查看不扣减、编辑、模拟失败零变化、保留编辑、原子成功和页面数值一致。
- **计划偏差**：无。数量未知批次默认跳过，避免把无法计算的余量伪装成精确扣减。

### 步骤 6：完成 Portfolio 视觉升级与质量验收

- **状态**：已完成
- **目标**：在不改信息架构的前提下形成温暖、克制、有食欲且适合截图展示的正式 Demo 视觉。
- **修改范围**：升级暖黄色/奶油白/少量橙色与自然绿色 Token；精修首页、CTA、食材卡、四态、推荐卡、菜谱、盲盒和反馈状态；添加少量本地压缩视觉资源；更新 `tests/portfolio-mvp.spec.mjs` 和必要开发测试配置。
- **验收方式**：390×844、窄屏和桌面均无遮挡或横向溢出；四态不只依靠颜色；Reduce Motion、键盘、焦点和屏幕阅读器语义保持可用；五段主闭环、四态、持久化、扣减失败恢复和无结果恢复自动化通过；无控制台错误。
- **实际完成**：建立暖黄、奶油白、克制橙色和自然绿色视觉 Token；精修首页主视觉、CTA、库存卡、四态符号、盲盒、结果、菜谱、扣减和完成反馈；加入显式且持久化的 Reduce Motion；补充测试配置与本地视觉验收截图。
- **测试结果**：Portfolio MVP 自动化 82/82（存储 5、推荐 15、扣减 10、浏览器主流程 30、质量 22）；320/390/768/1440px 无横向溢出，固定操作区不遮挡末端内容，可见操作区不小于 44px，键盘焦点、系统/显式 Reduce Motion 和控制台错误检查通过；原 `prototype/` 仍为 76/76。
- **计划偏差**：无。视觉资源采用轻量 HTML/CSS 图形而非新增图片依赖；步骤 7 的正式产品截图、README、仓库和部署仍未执行。

### 步骤 7：开源包装、截图与静态部署

- **状态**：Step 7A 已完成；外部 GitHub 操作待批准
- **目标**：使项目可公开访问、可从 GitHub 阅读和复现，并适合简历/作品集引用。
- **修改范围**：创建 `README.md`、`LICENSE`、`.gitignore`、Pages workflow 和产品截图；README 如实填写项目简介、Live Demo、核心功能、设计思路、范围、实现、本地运行、结构、Roadmap 与限制。
- **验收方式**：全新克隆后按 README 可启动和测试；仓库扫描无 secret、API Key、手机号或个人隐私；GitHub Pages 部署成功且刷新/子路径可用；README 截图与 Live Demo 链接有效；未实现能力只出现在 Roadmap。
- **实际完成（Step 7A）**：补充 Python 本地 HTTP 启动说明；创建 README、MIT LICENSE、.gitignore、Pages Actions workflow、稳定截图目录、部署烟测与发布预检；公开 artifact 仅包含 `index.html` 和 `src/`。
- **测试结果（Step 7A）**：Portfolio MVP 82/82、prototype 76/76、HTTP/Pages 子路径部署烟测 13/13、发布预检 13/13；实际 HTTP 打开、ES Modules、LocalStorage、刷新、主流程及零控制台错误均通过。
- **计划偏差**：无。真实 GitHub Pages 部署尚未执行，符合 Step 7A 停止条件；README 的 Live Demo 仍为待发布占位地址。

## 5. 明确推迟到 Roadmap

- 手机号验证码登录、用户账户、云数据库、游客与云端合并、跨设备同步。
- AI 拍照识别、受限制 AI 辅助和 AI 菜谱生成。
- 外出用餐完整流程、定位、地图、商圈、真实餐厅、外卖与第三方 POI。
- 第三方营养与实时食材价格 API；Portfolio 仅展示明确标注的 Mock/估算数据。
- 不参考库存的完整随机菜单采购流程、买菜清单和采购入库。
- 收藏、长期反馈权重、永久屏蔽、公开/私有自定义菜谱完整管理。
- 家庭共享库存、提醒推送、微信小程序、微信登录与多城市支持。
- 内部内容管理后台、社区、商业化和生产级推荐基础设施。

## 6. 范围保护

- 不因视觉升级改变已通过验证的核心流程、库存四态、盲盒节奏和确认后扣减规则。
- 不引入后端、数据库、API 或生产级推荐算法。
- 不把 Mock 营养、价格或菜谱规模描述为实时或完整数据。
- 不在本阶段加入与 Portfolio 核心闭环无关的页面或依赖。
